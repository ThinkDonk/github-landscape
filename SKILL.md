---
name: github-landscape
description: 调研 GitHub 上与项目想法或需求相似的公开仓库，比较功能覆盖、维护证据与限制。用于“这个想法有人做过吗”“调研同类开源项目”“GitHub 有哪些类似项目”等公开项目横向调研。不用于单仓库代码审计、可直接引入的依赖选型或学术论文调研。
---

# GitHub Landscape

找出与需求相关的公开项目，说明已覆盖的能力、可证实的限制与仍需核实的问题，帮助用户决定复用、改造或另做哪些部分。结论限于本次检索覆盖的项目；GitHub 供给不能单独证明市场需求或市场饱和。

## 获取证据

优先使用相对本 skill 目录的 `scripts/gh.py`，仅依赖 Python 标准库。复用搜索已返回的元数据，避免为同一张对比表重复请求。脚本内容不足时，可用现有连接工具、GitHub CLI、官方 API 或项目文档补证；不要因为脚本没有覆盖某个端点就停止调查。

```text
python scripts/gh.py search "<query>" --top 30
python scripts/gh.py search "<query>" --sort best
python scripts/gh.py search "<query 1>" "<query 2>" --sort best --top 30
python scripts/gh.py search "<query 1>" "<query 2>" --format json
python scripts/gh.py inspect owner/repo
python scripts/gh.py inspect owner/repo --format json
python scripts/gh.py rate
```

`inspect` 返回有限样本：release 页面、默认分支提交、贡献者、README 前缀，以及按更新时间排序的 open issue/PR 页面。它不会翻页，也不是完整仓库审计。报告中的关键判断若依赖被截断的 README、未展示的功能或完整历史，只补查能改变结论的部分。

需要程序复用或保存结构化证据时，三个命令均可用 `--format json`；默认仍为 Markdown。JSON 包含 `schema_version`、UTC 采集时间、来源 URL、实际请求数和取证状态。先检查 `status` 与样本边界，再读取数据：`null` 表示未取得，成功取得的空列表/空文本才表示本次返回为空。`status: ok` 只表示请求成功，不证明覆盖完整。JSON 中 releases 保留原始发布状态，仍需按下文规则判断已发布版本。

## 搜索与候选选择

- 从需求提炼核心功能、目标用户和部署/使用约束；仅在缺失信息会改变候选范围时澄清。
- 通常先用 2–4 组不同关键词搜索，可按项目语言补中文关键词。不要在第一轮用 push 日期或 stars 门槛排除成熟、小众项目。
- 合并去重后按需求相关性选择深入对象，通常 3–5 个即可；有意义时保留不同规模、维护模式或架构的代表，不凑数量。
- `search` 可接收多条查询，按仓库 ID 合并去重，保留 `Q1` 等命中来源；`--top` 对每条查询生效，不限制合并后的总数。结果按首次出现顺序展示，多次命中不是适配评分。重复查询仅执行一次。
- 结果偏少或全是热门项目时，换同义词、拆功能词、放宽限定符，或用 `--sort best` 补查。不要把一次搜索无结果写成“没有同类项目”。

## 分开评价维护与质量

时间、stars 和存档状态是证据，不是综合评分：

| 证据 | 可以说明什么 | 不能直接推出什么 |
|---|---|---|
| `archived` 或维护者的停止维护声明 | 当前维护边界；记录原文出处 | 代码完全无用或不值得参考 |
| push 日期、提交样本 | 近期仓库活动；检查是否主要是机器人、文档或依赖更新 | 产品质量高、核心功能仍在开发 |
| 有 `published_at` 的非草稿 release | 该版本已发布；区分正式版和预发布 | 当前样本就是全仓库最新正式版 |
| 长时间无提交或发布 | 更新低频，需结合成熟度、响应和实际适用性判断 | 项目已经停止维护 |
| stars、forks、贡献者 | 关注度和可见协作线索 | 稳定性、实现质量或需求适配程度 |
| 许可元数据、README、测试/CI、安装说明 | 可进一步核查的许可、文档与工程证据 | 缺失 API 字段等于没有许可；有徽章等于测试有效 |

综合有证据的维护者声明、变更内容、发布与响应情况给出维护判断。信息不足就写“维护状态未确认”。质量按任务相关的可安装性、文档、测试、功能和架构约束描述；只检查会影响选择的项目，不用固定 star 档位代替质量。

## 功能、限制与缺口

- 从用户需求选出真正需要比较的功能维度，使用“已证实支持 / 部分支持 / 已证实不支持 / 未确认”。
- README 未提及、摘要被截断、搜索未找到都属于“未确认”，不能直接标为缺失。否定能力需要明确文档、维护者回复、相关实现或可复现行为。
- issue 是样本和个案线索。记录具体链接、状态、日期和上下文；不要把用户报告直接当作已复现缺陷，也不要把最近更新样本中的重复话题写成“高频抱怨”。频率结论需要明确检索范围、去重方式和统计分母。
- GitHub 的 `open_issues_count` 包含 PR；issues 端点也混合返回 issue 与 PR。过滤后为空只说明本页没有 issue 样本，不能证明仓库没有 open issue。
- 技术栈名称本身不是缺点。只把与需求相冲突、且有直接证据的约束列为限制。

## 输出

默认用用户的语言在对话中给出结果。需要完整可复用报告或用户要求落盘时，保存到当前任务允许的交付目录；未指定目录时可用工作区内 `outputs/landscape-{slug}-{YYYYMMDD}.md`，不要写入 skill 安装目录。

按任务大小组织内容，至少说明：检索日期与范围、代表候选及其定位、功能覆盖、维护和质量证据、具体限制、结论与未确认项。项目和关键主张附直接来源链接，事实、作者声明与分析推断在对应段落区分。

结论可以是当前公开供给已覆盖主要需求、存在已证实的需求缺口，或证据不足。说明在什么条件下适合复用、改造或自建，不强制三选一；“是否值得商业化”还需要目标用户、闭源竞品、成本等证据。

## 请求与失败边界

- 无重试时，`search` 每条不同查询为 1 次 HTTP 请求，`inspect` 为 6 次，`rate` 为 1 次；每个端点最多尝试 5 次。脚本在标准错误输出本次命令的实际 HTTP 尝试数，包含失败和限速重试，不自动汇总多条命令。
- 多查询搜索保留成功结果及每条查询状态；有失败或跳过时退出码为 1。连接、认证或限额故障会停止后续查询并标记跳过；查询本身失败则继续其他查询。API 标记 `incomplete_results` 时会提示，不把局部结果当作完整检索。
- `inspect` 同样保留已取得的部分证据；端点不可访问、失败或跳过时退出码为 1。JSON 模式的 API 故障仍输出可解析的结果，诊断和请求计数写入标准错误；参数错误不保证 JSON 输出。
- 2–4 次搜索加 3–5 个仓库 inspect 的基线为 20–34 次请求；补查、限额查询和重试另计。这是估算，不是脚本强制限额。用户指定请求预算时，先按此成本缩小范围并预留重试空间，不承诺自动遵守未实现的硬上限。
- `GITHUB_TOKEN` 可选。限额以响应和 `rate` 输出为准，不假设匿名额度恒定。不要要求用户在对话里发送 token。
- 遇到权限、限额或连接限制，记录缺失证据并利用已有数据；停止重复失败的请求，不把不可访问解读为项目不存在或没有该能力。
